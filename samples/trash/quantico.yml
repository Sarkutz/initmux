---
name: Quantico
root: /i/project/quantico

windows:

- Qubit     : load
- Gates     : load

- Makefile : 

    panes:
        - Neovim:
            - mkdir .VWS
            - touch .VWS/<WName> 
            - nvim -c "VWSLoadWorkSpace <WName>"

        - ranger /i/project/quantico /i/project/Makefiles
models: 

    load:
        panes:
            - Neovim:
                - mkdir .VWS
                - touch .VWS/<WName> 
                - nvim -c "VWSLoadWorkSpace <WName>"

            - ranger
